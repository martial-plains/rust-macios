use crate::objective_c_runtime::macros::object;

object! {
    /// A definition of logical conditions for constraining a search for a fetch or for in-memory filtering.
    unsafe pub struct NSPredicate;
}
